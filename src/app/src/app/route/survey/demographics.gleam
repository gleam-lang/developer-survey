// IMPORTS ---------------------------------------------------------------------

import lustre/element.{Element}

// RENDER ----------------------------------------------------------------------

pub fn render() -> Element(action) {
  element.div([], [element.text("this is the `Survey/Demographics` route.")])
}
